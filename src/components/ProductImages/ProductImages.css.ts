import { style } from '@vanilla-extract/css'

export const imagesCont = style({
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
})

export const previewImage = style({
  borderWidth: 2,
  borderStyle: 'solid',
  borderColor: 'transparent',
})
